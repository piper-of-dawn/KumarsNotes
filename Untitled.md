
• Yes. The biggest improvement would be an on-screen cheat sheet the moment keyboard mode starts.

  High-value changes:

  - Show a small hint panel near the bottom: Arrows move, Shift+Arrows move faster, Ctrl+Arrows resize top/right, Ctrl+Alt+Arrows resize left/bottom, Enter capture, Esc cancel.
  - Add a visible crosshair or center dot inside the selection so movement feels more anchored.
  - Display the current geometry live: x, y, width, height.
  - Add a tiny step indicator: move: normal or move: fast when Shift is held.
  - Support Tab to cycle resize focus between edges, then use plain arrows to resize the active edge. That is easier to learn than many modifier combos.
  - Add + / - as simple grow/shrink shortcuts for proportional resizing.
  - Add Space to toggle between move mode and resize mode. One modifier-less mode switch is often easier than remembering multiple combinations.
  - Show a short flash or label when entering keyboard mode: Keyboard mode.
  - Let Backspace reset the selection to centered default.
  - If you keep the current bindings, document them directly inside the UI, not only in the README.